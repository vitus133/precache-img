apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: pre-cache-role-binding
  namespace: pre-cache
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: pre-cache-role
subjects:
- kind: ServiceAccount
  name: default
  namespace: pre-cache